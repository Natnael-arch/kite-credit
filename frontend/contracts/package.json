{
  "name": "kitecredit-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for KiteCredit lending protocol",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat run deploy.js --network kiteTestnet",
    "deploy:local": "hardhat run deploy.js --network localhost",
    "node": "hardhat node",
    "test": "hardhat test",
    "verify": "hardhat verify --network kiteTestnet"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "@openzeppelin/contracts": "^4.9.6",
    "dotenv": "^16.0.0",
    "hardhat": "^2.12.0"
  }
}
